# test-support-tools

Mini training project for learning Git and GitHub.

## Project purpose
This repository contains simple support tools for service monitoring.

## Files
- `check_status.sh` — checks service status
- `contacts.txt` — support notes and contacts

## Usage
Run the script with:

```bash
./check_status.sh